// 1. On importe la base de donnee et le DataTypes
import database from "../config/connection.js";
import { DataTypes } from "sequelize";

// 2. On cree le model "Inscription" dans la base de donnees avec ses attributs

// 3. On exporte le model "Inscription" pour lui creer un controller
